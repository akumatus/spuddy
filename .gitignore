claude-design/
3d-models/
# generated media (promo audio/video renders etc.) — large binaries, never for git
outputs/
.DS_Store
.claude/settings.local.json
web/.wrangler/
*.p12
*.p8
*fonts.html
# local scratch: README.md rendered through GitHub's own markdown API, to eyeball
# image sizes and the motion table before pushing. Regenerate, don't commit.
.readme-preview.html
